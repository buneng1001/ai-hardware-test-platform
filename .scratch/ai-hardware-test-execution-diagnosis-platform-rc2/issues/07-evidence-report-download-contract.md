# 07 — 完善报告、证据 ZIP、原始视频下载和人工结果契约

**What to build:** 让测试工程师获得可独立复查、边界清晰且可回导的报告与证据产物，同时区分完整原始视频和受限证据样本。

**Blocked by:** 03 — 实现独立时间对齐与逐帧视频—IMU 映射；06 — 让导入型任务进入统一运行与分析链路

**Status:** completed
**Version:** v0.1.0-rc.2

- [x] 原始视频提供独立下载；证据 ZIP 默认排除原始视频，仅允许显式加入受限小样。
- [x] ZIP 包含报告、结构化检查、人工结果、日志、缩略图、清单、哈希和逐帧映射，并记录导出时间。
- [x] ZIP 按下载时当前状态生成，人工结果或诊断变化后能提示重新导出。
- [x] 面向 Excel 的 CSV 使用 UTF-8 BOM；人工结果导入与导出契约一致，或提供明确独立模板。
- [x] 报告同时展示确定性检测结果和人工检查结果，不互相覆盖；缺少可选证据明确展示未执行。
- [x] 公开 API 测试覆盖默认排除、受限小样、敏感字段、哈希清单、可回导和失败不留半成品。

## Answer / 验证证据

- 新增 `/api/runs/{run_id}/videos/{channel}` 独立原始视频下载；默认证据 ZIP 排除原始视频，显式 `include_sample=true` 时只加入受限小样，并保留缩略图。
- 证据清单记录运行状态、导出时间和按当前状态生成的重导提示；报告继续独立保存自动检查与人工结果。
- `checks.csv` 和 `manual-check-results.csv` 使用 UTF-8 BOM；人工结果 CSV 列与现有导入契约一致，可通过公开导入 API 回导。
- 后端报告/证据/逐帧映射测试通过；新增测试覆盖视频下载、清单元数据、缩略图、小样边界、BOM、回导、敏感字段和失败无半成品。
- 验证结果：后端 `119 passed`；前端 `5 test files, 28 passed`；TypeScript、Prettier 和 Ruff 检查通过。
- 完成本 ticket 的只读 Standards/Spec code review，未发现需阻断提交的问题。
