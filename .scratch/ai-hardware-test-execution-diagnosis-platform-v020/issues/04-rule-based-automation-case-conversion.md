# 04 — 通过规则服务生成自动化用例候选

**What to build:** 从原始测试用例生成独立、可编辑的自动化用例候选。业务通过 `AutomationCaseConversionService` 调用确定性规则实现；无模型、无 Skill 和无外网时，该流程仍能完整运行。

**Blocked by:** 03 — 导入并管理原始测试用例。

**Status:** ready-for-agent

**当前前端 Demo 对应页面：** “自动化用例表”和“生成自动化用例”。

- [ ] 转换不会修改或覆盖原始测试用例。
- [ ] 每条自动化用例获得系统生成、不可编辑且不复用的唯一编号。
- [ ] 自动化用例只保留一个可点击的来源用例编号，可返回对应原始用例。
- [ ] 规则实现输出统一结构的候选内容、低可信度状态和人工审核提示。
- [ ] 单行缺少必要信息或转换失败不会使整批失败，失败行具有明显视觉状态和简短原因。
- [ ] 服务边界允许未来替换实现，但 v0.2.0 不依赖具体 Skill、Skill 文件格式或供应商 SDK。
- [ ] 数据库、转换接口、自动化用例表、规则样例和无 AI 环境均有自动化测试。
