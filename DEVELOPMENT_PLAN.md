# 分阶段开发计划

## 执行原则

- 从最短可运行链路开始，每阶段都必须产生可观察证据。
- 先做确定性生成与检查，再接入 AI。
- 每阶段完成时同步课程映射、工程日志和面试案例。
- 没有测试或报告证据的功能不标记为“已验证”。
- 所有实现均遵守洁净重写边界。

## 阶段 0：工程骨架与安全基线

### 目标

建立前后端、测试、配置和 CI 的最小工程骨架。

### 工作

- 建立 Python/FastAPI 后端和 React/TypeScript 前端。
- 配置 pnpm、Python 虚拟环境、Pytest、前端测试和格式检查。
- 添加 `.editorconfig`、`.gitattributes`、`.gitignore`、`.env.example`。
- 建立 SQLite 迁移方式和项目内数据目录。
- 配置 GitHub Actions Mock 流程。
- 编写洁净重写与敏感信息检查清单。

### 验收证据

- 后端健康检查和前端首页可运行。
- 后端、前端测试及 CI 通过。
- 仓库扫描不包含真实 Key 和公司资产。

## 阶段 1：任务模型与本地执行器

### 目标

跑通“创建任务—生成运行记录—后台执行—完成/失败/取消”的状态链路。

### 工作

- 实现采集任务、运行记录和阶段事件模型。
- 实现单执行任务队列和进度查询。
- 实现安全取消、异常中断识别和重新执行。
- 建立仪表盘、新建任务和运行详情的最小页面。

### 验收证据

- 状态迁移单元测试和 API 集成测试通过。
- 应用重启能识别未完成运行。
- 重新执行不会覆盖历史记录。

## 阶段 2：确定性合成数据生成器

### 目标

用固定随机种子生成可重复的视频、IMU、状态和日志。

### 工作

- 实现快速、标准和自定义参数校验。
- 通过 FFmpeg 生成 1～4 路 H.264 视频及共同事件。
- 生成 CSV/JSONL IMU 和模拟冲击峰值。
- 生成资源指标、设备日志和故障真值。
- 实现真实短数据与虚拟长时指标的来源标记。
- 实现六个内置场景。

### 验收证据

- 同一随机种子生成相同哈希或等价指标。
- 六个场景的故障真值固定且可读。
- 参数边界、非法输入和文件大小保护测试通过。

## 阶段 3：确定性检查器

### 目标

建立不依赖 AI 的视频、IMU、资源、日志和文件检查能力。

### 工作

- 检查视频路数、编码、帧率、时长、掉帧和坏帧。
- 检查 IMU 采样率、丢样、重复、倒退和间隔分布。
- 检查存储不足、提前停止和资源异常。
- 关联日志事件和失败时间窗口。
- 实现检查器统一结果 Schema。

### 验收证据

- 正常场景不误报。
- 五个故障场景均命中预期检查。
- 检测结果与故障真值对照测试通过。

## 阶段 4：时间对齐与同步分析

### 目标

展示 POC/EVT 原始差异、对齐过程和对齐后残差。

### 工作

- 实现参考通道选择和锚点展示。
- 实现固定偏移校正。
- 实现线性漂移校正。
- 输出对齐前偏移、抖动和漂移指标。
- 输出对齐后最大值、平均值、P95 和趋势。
- 分开检查时间戳同步和画面内容同步。
- 实现三种阈值来源与判定模式。

### 验收证据

- 已知偏移和漂移参数可被正确估计。
- 原始证据不被校正结果覆盖。
- 摸底模式不产生合格性结论。

## 阶段 5：人工结果与报告

### 目标

把自动化事实、人工结果和证据组织成可交付报告。

### 工作

- 实现人工检查录入及 CSV/Excel 导入。
- 完成分析报告页面和趋势图。
- 导出独立 HTML。
- 导出带文件清单、哈希、关键帧和结构化数据的 ZIP。
- 集成 Allure 展示项目自身测试结果。

### 验收证据

- 通过、失败、阻塞、未执行状态不被混淆。
- HTML 可脱离应用打开。
- ZIP 内容与运行记录一致，且不包含 Key 和默认原始视频。

## 阶段 6：硅基流动诊断与 Mock

### 目标

加入有证据、可校验、可降级的 AI 辅助诊断。

### 工作

- 实现模型服务适配层和硅基流动连接测试。
- 构建有 Token 上限的诊断证据包。
- 实现结构化输出 Schema 和证据引用校验。
- 实现诊断历史、Prompt 版本和模型元数据。
- 实现按错误类型区分的最多两次重试。
- 实现固定、可重复的 Mock 响应。

### 验收证据

- 真实 API 可在本地生成结构化诊断。
- Mock 模式的 CI 稳定通过。
- 模型失败不改变测试完成状态。
- 无效证据编号和越权结论会被拒绝或标记。

## 阶段 7：AI 效果评估与面试闭环

### 目标

证明 AI 诊断质量，并形成可复现的面试案例。

### 工作

- 对照六个场景的故障真值计算命中、漏判和无证据推测。
- 完成仪表盘统计和 AI 诊断评估展示。
- 编写一键启动、Mock 演示和真实 API 配置说明。
- 固化正常场景与组合故障场景的演示脚本。
- 更新课程映射、面试 STAR 和高频追问。
- 从工程日志选择 3～5 个真实难题形成面试材料。

### 验收证据

- README 可让新用户在 Mock 模式下复现。
- GitHub Actions、Pytest、前端测试和 Allure 全部通过。
- 首版验收标准逐项具有链接或产物证据。
- 面试文档中的量化结果均来自真实运行。

## 后续项目与扩展

主项目完成后，再洁净重写“测试设计与治理平台”，通过稳定 JSON/YAML 契约向主项目输出测试任务，并接收运行结果用于覆盖率和回归分析。

可选扩展包括音频、蓝牙、MCAP、真实设备适配、AI 接口专项、日志异常专项、回归选择和 Jenkins 对照实验。这些内容不进入首版完成条件。

