# AI 智能硬件测试执行与诊断平台规格

## 1. 目标

建设一套可在无真实设备、无公司数据的条件下运行的本地测试平台，展示 Python 自动化测试、智能硬件数据质量分析、前后端工程化和可验证的 AI 辅助诊断能力。

项目面向 Python 自动化测试工程师岗位，并兼顾智能硬件或嵌入式测试场景。实现必须遵守洁净重写边界。

## 2. 非目标

- 不复刻任何原公司产品、协议、界面或内部流程。
- 不使用原公司的代码、Prompt、数据、日志、截图或文档。
- 首版不接入真实设备，不实现音频、蓝牙和真实 MCAP。
- 不建设多人账户、权限、审批和复杂用例管理。
- 不让 AI 控制设备、修改事实或决定确定性检查结果。
- 不实现复杂非线性时间扭曲。

## 3. 使用者与运行环境

首版使用者为单个测试工程师，应用在本机运行。同一时间只允许一个测试任务执行，并允许任务排队、安全取消及异常中断识别。

## 4. 技术路线

- 后端：Python、FastAPI、Pytest。
- 前端：React、TypeScript。
- 数据库：SQLite。
- 视频：FFmpeg、ffprobe。
- AI：硅基流动为默认服务，通过独立适配层调用，并提供 Mock 模式。
- 自动化报告：Allure。
- 持续集成：GitHub Actions，CI 默认不调用真实模型。

## 5. 核心领域对象

- **采集任务**：保存可重复使用的场景与参数配置。
- **运行记录**：保存某次执行的配置快照、状态、证据和结果，不可被重新执行覆盖。
- **故障真值**：记录合成数据中预先注入的故障及预期检测结果。
- **检测结果**：确定性检查器计算出的指标和状态。
- **人工检查结果**：人工记录的通过、失败、阻塞或未执行状态及附件。
- **诊断证据包**：提供给模型的限量、可引用上下文。
- **诊断运行**：独立保存一次模型调用的配置、输入摘要、输出和状态。

详细术语以 [CONTEXT.md](./CONTEXT.md) 为准。

## 6. 数据生成

### 6.1 通道

- 1～4 路视频，默认 4 路。
- 1 路 IMU。
- 设备状态指标：CPU、内存、温度、存储等。
- 设备日志。

### 6.2 配置

- 模式：快速、标准、自定义。
- 时长：2～300 秒；超过范围的长稳场景使用虚拟时间。
- 分辨率：640×360、1280×720、1920×1080。
- 帧率：15、24、25、30、60 FPS。
- 视频容器：MP4、MKV；编码固定 H.264。
- IMU 格式：CSV、JSONL。
- IMU 采样率：50、100、200、500Hz。
- 随机种子：可配置，保证数据可重复生成。

### 6.3 混合模式

平台真实生成短视频和对应 IMU 数据；长时间温升、漂移和存储变化通过虚拟时间生成。报告必须标记数据是实际生成还是虚拟时间模拟。

## 7. 内置场景

1. 正常采集。
2. 单路视频掉帧。
3. 多路时间戳漂移。
4. IMU 丢样、重复或时间戳倒退。
5. 存储不足导致录制提前停止。
6. 温度升高后视频掉帧和 IMU 丢样的组合故障。

每个场景必须具有固定随机种子、故障真值和预期检测结果。

## 8. 时间分析

原始时间戳允许存在固定偏移、抖动、漂移和不同起点。平台不得假设正常数据天然对齐。

首版提供：

- 固定偏移校正。
- 基于多个共同事件的线性漂移校正。
- 默认以第 1 路相机为参考时钟，并允许选择其他通道。
- 视频使用时间码或跑马灯事件，IMU 使用模拟冲击峰值作为跨模态锚点。
- 自动识别锚点，并允许查看和调整。

报告必须同时保留对齐前指标、对齐方法及参数、对齐后残差。时间戳对齐与画面内容同步必须分别评价。

## 9. 判定模式

- **需求验收模式**：使用正式规格并允许给出合格或不合格结论。
- **工程目标模式**：按 POC/EVT 当前目标评价，明确不代表产品承诺。
- **摸底分析模式**：只有版本基线时展示分布和趋势，不输出合格性结论。

判定依据优先级为正式规格、工程目标、版本基线。阈值来源必须进入报告，AI 不得生成阈值。

## 10. 人工测试结果

- 支持新增人工检查项。
- 状态包括通过、失败、阻塞、未执行。
- 支持实际结果、备注、执行时间和小型附件。
- 支持统一模板的 CSV/Excel 导入。
- 人工结果与自动化结果独立保存，在报告层统一汇总。

## 11. AI 诊断

### 11.1 输入

诊断证据包包含任务配置、阈值来源、失败指标、异常时间窗口日志、同期资源指标、关键帧或视觉分析结果、IMU 摘要及人工结果。每条证据具有稳定编号，且受大小和 Token 上限约束。

### 11.2 输出

模型必须返回通过 Schema 校验的 JSON：

- 诊断状态。
- 异常现象。
- 可能原因和证据引用。
- 高、中、低置信级别。
- 影响范围。
- 复测建议。
- 缺失证据。
- 限制与不确定性。

无证据支持的内容只能标记为推测。自由文本只能由结构化结果渲染得到。

### 11.3 评估

合成场景下将诊断原因与故障真值对照，统计命中、漏判和无证据推测。结构校验与效果评估必须分开。

### 11.4 降级

模型服务不进入测试关键路径。硅基流动不可用时，生成、检查和原始报告仍须完成。只对超时、限流和临时服务错误自动重试，最多两次；其他错误直接保留并允许人工重试。

## 12. 状态模型

测试执行状态：草稿、排队、生成数据、执行检查、汇总结果、已完成、执行失败、已取消、异常中断。

AI 诊断状态：待生成、生成中、已完成、生成失败。

两类状态独立保存。重新执行创建新的运行记录，重新诊断创建新的诊断运行。

## 13. 页面

1. 仪表盘：统计、近期失败和诊断状态。
2. 新建任务：配置数据、场景、阈值和对齐方法。
3. 运行详情：阶段、进度、日志、取消和重新执行。
4. 分析报告：指标、图表、人工结果、证据、诊断和评估。
5. 设置：硅基流动、连接测试、数据目录和 Mock 模式。

## 14. 报告与证据

- 应用内交互报告。
- 可独立打开的 HTML 报告。
- ZIP 证据包：JSON、CSV、人工结果、日志、缩略图、文件清单、哈希和报告。
- Allure 用于展示项目自动化测试结果。
- 原始视频默认不进入 ZIP，只包含关键帧和可选小样。
- AI 摘要必须记录模型、生成时间、Prompt 版本和 Mock 状态。

## 15. 密钥与日志安全

- 默认从后端 `.env` 读取硅基流动 API Key。
- 前端临时输入只保存在当前会话内存。
- SQLite、日志、报告和证据包不得保存 Key。
- 后端不向前端返回 Key 或掩码值，只返回是否已配置。
- 日志过滤认证头和敏感字段。
- 仓库只提交 `.env.example`。

## 16. 首版验收标准

- 六个场景均能通过界面执行。
- 三种数据模式和规定的自定义参数有效。
- 真实生成短视频、IMU、指标和日志。
- 两种对齐方法可运行且保留对齐前后结果。
- 正常场景不误报，五个故障场景均命中预期确定性检查。
- 三种判定模式符合定义。
- 人工与自动化结果独立保存并统一展示。
- 硅基流动输出通过 Schema 和证据引用校验。
- AI 诊断可对照故障真值评估。
- 模型不可用时测试和原始报告仍成功。
- HTML、ZIP、Allure 和 CI 产物可验证。
- Mock 模式支持面试官按 README 复现。
- 不包含原公司资产或敏感信息。

## 17. 持续维护文档

- [课程应用映射](./docs/course-coverage.md)：实现后同步状态与证据。
- [面试案例](./docs/interview-case.md)：只补充真实完成的能力和数据。
- [工程日志](./docs/engineering-journal.md)：按问题记录证据、根因、修复和验证。
- [架构决策](./docs/adr/)：仅记录难以逆转且存在真实取舍的决定。

