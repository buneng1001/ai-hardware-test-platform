# 主项目采用 Python 前后端分离架构

主项目后端采用 Python、FastAPI 和 Pytest，前端采用 React 与 TypeScript，首版使用 SQLite、FFmpeg/ffprobe、Allure 和 GitHub Actions。相比快速搭建的 Streamlit，这套方案开发量更大，但能同时展示 Python 自动化、接口测试、前后端分离和持续集成能力，更符合核心面试作品的目标。
